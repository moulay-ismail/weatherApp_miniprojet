<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.weatherapp_miniprojet">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/iconfinder_sun_simple_cloudy_snow_367523"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.WeatherApp_miniprojet"
        android:usesCleartextTraffic="true">
        <activity android:name=".Activities.aboutUsActivity"></activity>
        <activity android:name=".Activities.UpdateUniteActivity" /> <!-- usesCleartextTraffic :  Déclarez que cette application peut utiliser du trafic en texte clair, tel que HTTP plutôt que HTTPS; -->
        <activity android:name=".Activities.MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Activities.VilleFavorie" />
    </application>

</manifest>